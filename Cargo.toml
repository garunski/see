[package]
name = "simple_workflow_app"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
dataflow-rs = "2.0.0"
datalogic-rs = "4.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
