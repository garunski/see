{
  "name": "s_e_e_icons",
  "version": "1.0.0",
  "description": "SVG icons for Speculative Execution Engine",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run build:branding && npm run build:heroicons",
    "build:heroicons": "node copy-icons.js",
    "build:branding": "node scripts/build.mjs",
    "verify": "node scripts/verify.mjs"
  },
  "dependencies": {
    "heroicons": "^2.2.0"
  },
  "devDependencies": {
    "@ctjs/png2icons": "^3.0.3",
    "sharp": "^0.33.0"
  }
}
